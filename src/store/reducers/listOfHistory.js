const initialState = {
  history: [],
};

export const listOfHistoryReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      console.log(1231);
      return state;
  }
};
